apply plugin: 'java'

repositories {
    jcenter();
}

dependencies {
    compile 'com.squareup:javapoet:1.3.0'
    compile 'com.bluelinelabs:logansquare-compiler:1.3.1'
    compile project(':core')
}